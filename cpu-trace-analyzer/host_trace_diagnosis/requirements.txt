# Host Trace Diagnosis - Python Dependencies
# ================================================
# Core dependencies (required)
pyyaml>=6.0

# Large file streaming JSON parsing (required for >100MB trace files)
ijson>=3.2

# Columnar storage for IR (optional, enables Parquet format)
# If not installed, IR falls back to JSONL format
pyarrow>=14.0

# Perfetto TraceProcessor integration (optional)
# Enables SQL queries on trace data
# perfetto>=0.18.0

# NOTE: Python standard library modules used:
# - json, csv, sqlite3, re, os, sys, pathlib, dataclasses, enum
# - collections, statistics, math, logging, argparse, tempfile
# - subprocess, heapq
