<!--
CPU Host 性能分析报告骨架模板
用途：
  1. 分析器无法完整运行时（数据缺失/格式不支持），Agent 按本模板人工撰写报告；
  2. 定制报告结构时保持与 analyzer/report.py 输出一致的 14 节结构。
规则：
  - 正文一律中文；指标英文名（如 NET_RX、sched_switch）保留。
  - 每个指标必须附"是什么"的中文解释，释义从 analyzer/metrics.py 的
    METRIC_EXPLANATIONS / EVENT_EXPLANATIONS / IRQ_NAME_EXPLANATIONS 取用。
  - 每个结论必须含 Metric / Value / Threshold / Evidence / Confidence（HIGH/MEDIUM/LOW）。
  - 占位符 {xxx} 填写后删除本注释块。
-->
# CPU Host 性能分析报告

> 分析对象：`{trace 文件名}` · 采集时长：`{duration}s` · 分析时间：`{YYYY-MM-DD HH:MM}`
> 工具：cpu_host_performance（ftrace 离线分析，仅标准库）

## 1. Executive Summary（结论摘要）

### Host 状态：{CRITICAL|WARNING|DEGRADED|HEALTHY|UNKNOWN}（{中文说明}）

### 核心问题
- {问题1：一句话结论，含关键数值}
- {问题2：...}

### 影响
{对训练/推理吞吐、NPU 利用率、数据加载等上层业务的影响，须有证据支撑}

### 解决方案（按优先级）
1. {动作1（优先级最高，写明具体命令）}
2. {动作2}

## 2. 问题证据总表

<!-- 每行一个结论：编号 | 问题 | 严重度 | 置信度 | 关键证据 -->
| # | 问题 | 严重度 | 置信度 | 关键证据 |
|---|------|--------|--------|----------|
| 1 | {问题名} | {严重/警告/提示} | {HIGH/MEDIUM/LOW} | {一行证据} |

## 3. 问题详情

<!-- 每个问题一个小节，固定五要素：现象 → 证据（表格）→ 阈值与解释 → 根因假设 → 建议 -->
### 问题 {N}：{问题名}（{严重度} / 置信度 {HIGH/MEDIUM/LOW}）

**现象**：{一句话说明发生了什么}

**证据**：
| 指标 | 值 | 阈值/判据 | 说明 |
|------|-----|-----------|------|
| {指标名} | {值} | {阈值} | {该指标是什么（中文释义）} |

**为什么重要**：{该模式对性能的影响链条}

**可能根因**：{按可能性排序，证据不足处明确写"证据不足"}

**建议动作**：
- {具体命令或操作}

## 4. CPU 总体状态

<!-- 总利用率、每核利用率分布、runnable 占比、idle 占比；异常核单独点出 -->
| CPU | 利用率 | runnable占比 | idle占比 | IRQ占比 | SoftIRQ占比 |
|-----|-------:|-------------:|---------:|--------:|------------:|
| {N} | {%} | {%} | {%} | {%} | {%} |

> 指标解释：{util / runnable / idle / irq_ratio / softirq_ratio 的中文释义}

## 5. CPU Core Balance（各核心负载）

<!-- 最大核 vs 最小核对比、不均衡度；结论写明是否 IRQ affinity/绑核导致 -->
{分析段落 + 判定}

## 6. Scheduler 调度分析

<!-- 调度延迟 avg/p50/p95/p99/max、runnable 任务数、迁移/抢占次数；top 延迟任务表 -->
| 指标 | 值 | 解释 |
|------|-----|------|
| 调度延迟 avg/p50/p95/p99/max | {ms} | {释义} |
| 迁移次数 / 抢占次数 | {n} | {释义} |

Top 延迟任务：
| 任务 | 最大延迟(ms) | 平均延迟(ms) | 次数 |
|------|------------:|------------:|-----:|
| {task (pid N)} | {} | {} | {} |

## 7. IRQ / SoftIRQ 分析

### 7.1 硬中断
| 中断名 | 次数 | 总耗时(ms) | 单次平均(μs) | 说明 |
|--------|-----:|----------:|-------------:|------|
| {name} | {} | {} | {} | {该中断是什么（中文释义）} |

### 7.2 软中断
| 软中断 | 次数 | 总耗时(ms) | 说明 |
|--------|-----:|----------:|------|
| {NET_RX} | {} | {} | {释义，如：网络收包软中断，过高说明收包压力大} |

<!-- 若软中断集中单核：写明占比并指向 IRQ affinity -->

## 8. CPU Frequency / Idle 分析

<!-- 每核频率 min/avg/max、低频样本占比；governor（如快照提供）；C-state 分布 -->
| 核心 | 最低 MHz | 平均 MHz | 最高 MHz | 低频样本占比 |
|------|--------:|--------:|--------:|------------:|
| {N} | {} | {} | {} | {%} |

> 频率单位由 trace 原始值换算（常见 kHz/Hz，已自动识别）；仅凭 trace 不判定硬件故障。

## 9. Task / Thread 分析

<!-- 累计运行时长 top 任务表；高频 wakeup 任务；dataloader/worker 类任务重点标注 -->
| 任务 | 运行时长(ms) | 占比 | 说明 |
|------|------------:|-----:|------|
| {task (pid N)} | {} | {%} | {角色推断} |

## 10. Affinity / NUMA 分析

<!-- trace 内可推断的绑核/迁移跨核信息；不能判断时明确写"证据不足" -->
{分析或"证据不足"声明}

## 11. 时间窗口分析

<!-- 问题发生的连续时间段、持续性 vs 突发、周期性；无时间聚合能力时说明局限 -->
{时间线描述}

## 12. 根因判断

<!-- 仅在证据充分时建立因果链，如：高负载 → NET_RX↑ → 单核打满 → 调度延迟↑ → worker 运行时间↓ -->
{因果链或主要根因排序}

## 13. 优化建议

<!-- 按预期收益排序，每条写明具体命令；区分"立即可做 / 需要更多信息" -->
1. {建议1（命令）}
2. {建议2}

## 14. 数据说明与指标解释

### 数据概况
- 事件总数：{N} · 解析格式：{ftrace文本|trace-cmd|JSON|...} · CPU 数：{N}
- 缺失/未覆盖事件：{列表或"无"}

### 分析限制
- {如：未采集 perf 快照，governor 未知；时间窗口仅 {N}s，无法判断长周期现象}

### 指标解释（每个指标是什么）
| 指标 | 含义 |
|------|------|
| {名称} | {中文释义，取自 METRIC_EXPLANATIONS} |

### 事件/术语解释
| 事件/术语 | 含义 |
|-----------|------|
| {sched_switch 等} | {中文释义，取自 EVENT_EXPLANATIONS} |
